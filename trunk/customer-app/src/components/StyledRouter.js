/*
import { Router } from 'react-native-router-flux'
import {ComponentsStyle} from '../configs/CommonStyles';

export default StyledRouter = (props) => (
    <Router
      navigationBarStyle={ComponentsStyle.NavBar.NavigationBarStyle}
      titleStyle={ComponentsStyle.NavBar.TitleStyle}
      barButtonIconStyle={ ComponentsStyle.NavBar.ButtonIconStyle}
      barButtonTextStyle={ ComponentsStyle.NavBar.ButtonTextStyle}
      { ...props }
    >
      { props.children }
    </Router>
)
*/
